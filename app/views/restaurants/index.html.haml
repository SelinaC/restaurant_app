%p Restaurants

- @restaurants.each do |rest|
  %ul
    %li
      %h4= link_to(rest.name, restaurant_path(rest))
      %p= rest.town
      %p= rest.postcode
      %p= rest.cuisine
      %p= rest.hygiene_rating
